# Backend API URL for production builds
# Set this to your deployed backend URL (e.g. Render, Railway, VPS)
# For local desktop-only use, leave empty or set to http://localhost:3001
VITE_BACKEND_URL=https://your-backend.onrender.com
